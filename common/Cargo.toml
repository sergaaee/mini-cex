[package]
name = "common"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
rust_decimal = "1.39.0"
redis = { version = "0.32.7", features = ["aio", "tokio-comp"] }